// Common/histogram_tools.h

namespace Histogram {
// Prints a histogram (array of length `bins`) to console with specific width and height
void print(unsigned* histogram, unsigned bins, unsigned width, unsigned height);
}